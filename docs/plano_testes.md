# Plano de Testes
    - Testar todos os métodos CRUD do usuário (Utilizar Mock)
        - 1 caso de sucesso e 1 caso de falha para cada
    - Testar todos os métodos de criação de consulta (Utilizar Mock)
        - Testar erros
            - Malformed JSON (JSON sem campos esperados)
            - Utilizar método (Post,Get,Delete, etc.) errado
    - Cobertura de pelo menos 75% do código
    - Seguro contra pelo menos 50% de casos de mutação criado pela lib mutmut
